Converting jpg to pdf, word to pdf, ppt to pdf, compressing size of pdf, compressing size of ppt 

Dependencies:

-Python 3

-OpenCV library (Computer Visual), install using:

pip install docx2pdf

pip install comtypes

pip install pymupdf

pip install pillow

